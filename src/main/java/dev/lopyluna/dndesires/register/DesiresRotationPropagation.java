package dev.lopyluna.dndesires.register;

public class DesiresRotationPropagation {
    static {
        //RotationPropagationRegistry.register(DesiresBlocks.OMNI_SPEED_CONTROLLER::get, new RotationPropagation() {
        //    @Override
        //    public Float getRotationSpeedModifier(KineticBlockEntity from, KineticBlockEntity to) {
        //        var toPos = to.getBlockPos();
        //        var fromPos = from.getBlockPos();
        //        var diff = toPos.subtract(fromPos);
        //        var face = Direction.getNearest(diff.getX(), diff.getY(), diff.getZ());
        //        return 1f;
        //    }
        //});
    }

    public static void register() {}
}
